:- module(lists, [length/2, member/2]).

:- chr_constraint length/2.

:- function member/2.

length([], R) <=> R = 0.
length([_|Xs], R) <=> length(Xs, R1), R is R1 + 1.

member(_, []) -> false.
member(X, [X|_]) -> true.
member(X, [_|Xs]) -> member(X, Xs).
