:- module(fib, [fib/2]).

:- chr_constraint fib/2.

base0 @ fib(0, R) <=> R = 0.
base1 @ fib(1, R) <=> R = 1.
rec @ fib(N, R) <=> N1 is host:'-'(N, 1), N2 is host:'-'(N, 2), fib(N1, R1), fib(N2, R2), Tmp is R1 + R2, R = Tmp.
