:- module(call_bare_atom, [go/1]).
:- use_module(library(prelude)).
:- chr_constraint go(any).

% A bare `'$call'` names the primitive with no callee at all; it must be
% rejected, not treated as a data atom.
r @ go(X) <=> X is '$call'.
