:- module(call_arity_zero, [go/1]).
:- use_module(library(prelude)).
:- chr_constraint go(any).

% A callee with no arguments. This is not a dynamic call the compiler
% supports, so it must be rejected rather than resolving to a data term.
r @ go(X) <=> X is '$call'(fun(A) -> A end).
