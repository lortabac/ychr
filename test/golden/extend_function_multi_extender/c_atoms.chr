:- module(atoms, [type(colors/0)]).
:- use_module(owner, [classify/1]).
:- chr_type colors ---> red.

% Second extender adds an overload over its own algebraic type.
:- extend_class_type (classify(colors) -> any).
:- extend_class classify(red) -> quote(color).
