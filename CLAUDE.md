# Project: YCHR

## Project description

An efficient CHR compiler with multiple backends.
See @PROJECT.md

## Resources

- The `docs` folder contains useful documentation,
  including the paper this compiler is based on.

## Formatting

After a task is completed, format the whole code base with
`ormolu -i $(find src test -name '*.hs')`.
